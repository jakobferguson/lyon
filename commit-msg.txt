fix(phase-2): address PR review — memory leak, form data, imports, styles

- Fix React import at bottom of IncidentDetailRoute (useState at top)
- Fix URL.createObjectURL memory leak in PhotoUpload (revokeObjectURL on remove/unmount)
- Wire PhotoUpload File objects into react-hook-form via setValue('photos', ...)
- Add photos: File[] field to IncidentFormValues and default values
- Fix onSubmit to accept IncidentFormValues param and log in dev mode
- Remove unused zod and @hookform/resolvers dependencies
- Disable Save Draft button with Tooltip explaining it needs API connection
- Extract STATUS_VARIANT and formatDateTime to shared incidents/utils.ts
- Extract DIVISIONS to shared types/index.ts; remove local duplicates
- Replace CompletionBar inline styles with CompletionBar.module.css
- Move .lyon-input base styles to index.css (global); remove :global blocks from FormField.module.css
- Remove redundant FormField wrapper around LocationPicker (it labels itself internally)
